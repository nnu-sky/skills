# 文件传输

仅在用户明确要求上传、下载、复制或同步文件时使用。

从本地配置解析目标 SSH 别名后，使用 `scp` 或 `rsync`。传输前确认来源、目标和目标磁盘空间；传输后核对文件数量、大小或校验值。训练进行中不覆盖活动检查点和日志。

同步 Codex 配置时：

- 不复制 `auth.json`、私钥、密码、令牌和本地服务器配置；
- 更新其他服务器的 `AGENTS.md` 前，先读取目标服务器已有规则和本地环境配置；
- 本地配置已明确实验环境时，使用该服务器自己的环境内容；没有环境说明时，省略实验环境章节。
