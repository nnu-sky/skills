# 安全说明

私有仓库不等于秘密保险箱。以下内容不得提交：

- 真实订阅地址和节点凭据；
- 控制器口令、访问令牌和密码；
- SSH 私钥、公钥授权清单和主机指纹记录；
- 服务器公网入口、账号和内部拓扑；
- Codex、ChatGPT 或其他应用的登录文件；
- 现网配置、运行缓存和回滚备份。

提交前运行：

```bash
bash scripts/check-sensitive.sh
```

如果敏感信息曾进入 Git 历史，仅删除当前文件不够。应立即轮换对应凭据，并重写 Git 历史后重新扫描。

公开仓库前还应人工检查提交历史、合并请求、议题附件和自动化日志。
